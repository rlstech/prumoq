import type { ContractorEvaluationReport } from './contractor-evaluation';

const esc=(value:string|null|undefined)=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
export function renderContractorEvaluationHtml(report: ContractorEvaluationReport) {
 const {evaluation,itens}=report; const date=new Date(`${evaluation.data_avaliacao}T12:00:00`).toLocaleDateString('pt-BR');
 const rows=itens.map(item=>`<tr><td class="num">${item.ordem}</td><td>${esc(item.titulo)}${item.resultado==='nao_atende'?`<div class="comment">${esc(item.comentario_nao_atende)}</div>`:''}</td><td class="center">${item.peso}</td><td class="center">${item.resultado==='atende'?'X':''}</td><td class="center">${item.resultado==='nao_atende'?'X':''}</td></tr>`).join('');
 return `<!doctype html><html><head><meta charset="utf-8"><style>@page{size:A4 portrait;margin:12mm}*{box-sizing:border-box}body{font:11px Arial,sans-serif;color:#142522}.sheet{border:1px solid #163B50;min-height:270mm}.head{display:grid;grid-template-columns:140px 1fr 92px;border-bottom:1px solid #163B50}.brand{padding:18px 12px;font-size:24px;font-weight:700;color:#163B50}.title{border-left:1px solid #163B50;border-right:1px solid #163B50;text-align:center;padding:18px 8px;font-size:14px;font-weight:700}.rev{padding:18px 8px;font-size:9px;font-weight:700;text-align:center}.meta{display:grid;grid-template-columns:1fr 1fr;gap:0;border-bottom:1px solid #163B50}.meta div{padding:7px 9px;border-right:1px solid #d0d7d2}.meta b,.label{font-size:8px;text-transform:uppercase;color:#52615B;letter-spacing:.06em;display:block;margin-bottom:3px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #7d8983;padding:6px;vertical-align:top}th{font-size:8px;text-transform:uppercase;background:#edf0eb}.num,.center{text-align:center}.comment{margin-top:5px;padding-top:5px;border-top:1px solid #d9ddd9;color:#52615B;font-size:10px}.total{font-weight:700;background:#f3f7d5}.sections{padding:9px}.section{border:1px solid #7d8983;margin-top:9px;min-height:56px}.section h2{margin:0;padding:5px 7px;font-size:9px;text-transform:uppercase;border-bottom:1px solid #7d8983;background:#edf0eb}.section p{white-space:pre-wrap;padding:7px;margin:0;min-height:35px}.sign{display:flex;justify-content:flex-end;padding:22px 22px 12px}.signbox{width:210px;text-align:center;border-top:1px solid #142522;padding-top:5px;font-size:9px}.signbox img{max-width:180px;max-height:65px;display:block;margin:-70px auto 5px}.invalid{position:fixed;inset:40% 0 auto;text-align:center;font-size:34px;font-weight:700;color:#B23A3A;opacity:.16;transform:rotate(-22deg)}</style></head><body>${evaluation.status==='invalidada'?'<div class="invalid">AVALIAÇÃO INVALIDADA</div>':''}<div class="sheet"><header class="head"><div class="brand">PrumoQ</div><div class="title">AVALIAÇÃO DE FORNECEDOR DE SERVIÇO<br><small>${esc(report.modelo)} · Rev. ${report.revisao}</small></div><div class="rev">EMITIDO EM<br>${date}</div></header><section class="meta"><div><b>Fornecedor / Empreiteiro</b>${esc(report.empreiteiro)}${report.cnpj?` · ${esc(report.cnpj)}`:''}</div><div><b>Obra</b>${esc(report.obra)}</div><div><b>Origem</b>${evaluation.medicao_id?'Medição de serviço':'Avaliação avulsa'}</div><div><b>Data da avaliação</b>${date}</div></section><table><thead><tr><th>Item</th><th>Critério de avaliação</th><th>Peso</th><th>Atende</th><th>Não atende</th></tr></thead><tbody>${rows}<tr class="total"><td colspan="2">TOTAL</td><td>${evaluation.pontos_possiveis}</td><td colspan="2">${evaluation.pontos_obtidos} pontos · ${Number(evaluation.percentual).toFixed(1)}%</td></tr></tbody></table><div class="sections"><section class="section"><h2>Notificações ocorridas</h2><p>${esc(evaluation.notificacoes_ocorridas) || 'Sem registros adicionais.'}</p></section><section class="section"><h2>Providências tomadas</h2><p>${esc(evaluation.providencias_tomadas) || 'Sem providências registradas.'}</p></section>${evaluation.status==='invalidada'?`<section class="section"><h2>Motivo da invalidação</h2><p>${esc(evaluation.motivo_invalidacao)}</p></section>`:''}</div><div class="sign"><div class="signbox">${report.signature?`<img src="${report.signature}" data-pdf-kind="signature">`:''}<b>${esc(report.avaliador)}</b><br>Responsável pela avaliação · ${evaluation.assinada_em?new Date(evaluation.assinada_em).toLocaleString('pt-BR'):''}</div></div></div></body></html>`;
}
